//import groovy.json.JsonSlurper
pipeline {
    agent any

    stages {
        stage('http-request') {
            steps {
                echo "Welcome to the new life Ramesh"
            }
        }
    }
}
